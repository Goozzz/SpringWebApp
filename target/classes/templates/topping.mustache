<html>
<head>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <title>KURSACH</title>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
</head>
<body>
<div>
    <form action="/logout" method="post">
        <input type="hidden" name="_csrf" value="{{_csrf.token}}" />
        <input type="submit" value="Sign Out"/>
    </form>
</div>
<header>

    <nav class="nav nav-tabs nav-justified">
        <a class="nav-item nav-link" href="/">Главная</a>
        <a class="nav-item nav-link" href="/user">Пользователи</a>
        <a class="nav-item nav-link" href="/orderingInformation">Заказы</a>
        <a class="nav-item nav-link" href="/worker">Сотрудники</a>
        <a class="nav-item nav-link active" href="/topping">Топпинги</a>
        <a class="nav-item nav-link" href="/coffee">Кофе</a>
    </nav>


</header>

<form style="padding: 10px" method="post"action="addCoffeeTopping">
    <div class="form-row">
        <div class="form-group col-md-3">
            <label for="inputCoffeeToppingDescription">Характеристика</label>
            <input type="text" name="coffeeToppingDescription" class="form-control" id="inputCoffeeToppingDescription" placeholder="Описание">
        </div>
    </div>
    <input type="hidden" name="_csrf" value="{{_csrf.token}}" />
    <button type="submit" class="btn btn-primary" >Добавить</button>
</form>

<hr>

<form style="padding: 10px" method="post"action="deleteCoffeeTopping">
    <div class="form-row">
        <div class="form-group col-md-1">
            <label for="inputCoffeeToppingId">Id</label>
            <input type="text" name="coffeeToppingId" class="form-control" id="inputCoffeeToppingId" placeholder="Id топпинга">

        </div>
    </div>
    <input type="hidden" name="_csrf" value="{{_csrf.token}}" />
    <button type="submit" class="btn btn-primary" >Удалить</button>
</form>

<hr>

<div>Список топпингов</div>
<div style="padding: 10px">
    <table class="table" >
        <thead>
        <tr>
            <th scope="col">Id</th>
            <th scope="col">Описание</th>
        </tr>
        </thead>
        <tbody>
        {{#coffeeToppings}}
            <div>
                <tr>
                    <td>{{coffeeToppingId}}</td>
                    <td>{{coffeeToppingDescription}}</td>
                </tr>
            </div>
        {{/coffeeToppings}}
        </tbody>
    </table>
</div>

</body>
</html>